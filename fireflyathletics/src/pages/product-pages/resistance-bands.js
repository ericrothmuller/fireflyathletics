import React from "react";

const ResistanceBands = () => {
    return (
        <main className="pageHeadingDiv">
        <h1 className="pageTitle">Resistance Bands</h1>
        <p className="pageParagraph">Product info goes here.</p>
        </main>
    );
};

export default ResistanceBands;